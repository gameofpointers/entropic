This directory is intentionally committed so Tauri bundle resource globs resolve in
development checkouts that do not yet have bundled runtime binaries.

Production/runtime bundle scripts must replace this placeholder with real
`colima`, `limactl`, `lima`, and `docker` artifacts.
