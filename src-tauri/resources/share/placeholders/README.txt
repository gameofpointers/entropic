This directory is intentionally committed so Tauri bundle resource globs resolve in
development checkouts that do not yet have bundled Lima/share assets.

Production/runtime bundle scripts must replace these placeholders with the actual
runtime templates and shared assets.
